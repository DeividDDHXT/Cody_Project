pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // I // sh 'python manage.py test'nstalar dependencias de Python
                sh 'pip install django
                
         
        }
        stage('Tests') {
            steps {
                // Ejecutar pruebas de Django
               
            }
        }
        stage('Deploy') {
            steps {
                // Detener servicios antiguos y reconstruir contenedores Docker
           echo 'no disponible'
            }
        }
    }
}
